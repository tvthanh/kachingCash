(function() {
    'use strict';

    angular.module('panelApp')
        .controller('cropperImageController', [
            '$scope',
            'kachingZonesHelpers',
            '$uibModal',
            function(
                $scope,
                kachingZonesHelpers,
                $uibModal
            ) {

            }
        ]);
})();
