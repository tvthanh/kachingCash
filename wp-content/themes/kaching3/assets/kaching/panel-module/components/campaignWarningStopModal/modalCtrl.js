(function(){
    'use strict';

    angular.module('panelApp')
        .controller( 'campaignWarningStopModalCtrl', [
            '$scope',
        function (
            $scope,
            $alert,
            campaignsService,
            callbacks
        ) {
        }]);
})();
